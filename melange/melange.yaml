package:
  name: hello-server
  version: 0.1.0
  epoch: 0
  description: friendly little webserver
  target-architecture:
    - all
  copyright:
    - license: Apache-2.0
      paths:
        - "*"
environment:
  contents:
    keyring:
      - https://packages.wolfi.dev/os/wolfi-signing.rsa.pub
      - ./melange.rsa.pub
    repositories:
      - https://packages.wolfi.dev/os
    packages:
      - ca-certificates-bundle
      - busybox
      - git
      - go
pipeline:
  - uses: fetch
    with:
      uri: https://github.com/maligin/partner-enablement-workshop/archive/refs/heads/main.tar.gz
      expected-sha256: ec27affa73600b4ac17f00cf2c1d939b9acf2da9b5d44b66a6785b093d198ddc
  - name: Build Go application
    runs: |
      CGO_ENABLED=0 go build -o "${{targets.destdir}}/usr/bin/hello-server" .
